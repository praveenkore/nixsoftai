benchmark: STIG
id: "V-269183"
title: "AlmaLinux OS 9 cron configuration files directory must be group-owned by root."
stig_id: "ALMA-09-011790"
original_severity: "CAT II"
severity: medium
cci: CCI-000366
srg: SRG-OS-000480-GPOS-00227

rationale: |
  Cron configuration files control scheduled tasks. If not group-owned by root, unauthorized users
  could modify them to execute malicious code with elevated privileges.

implementations:
  rhel_family:
    os: ["rhel", "centos", "almalinux", "rocky"]
    check:
      type: command
      command: ["stat", "-c", "%G", "/etc/cron.d", "/etc/cron.daily", "/etc/cron.hourly", "/etc/cron.monthly", "/etc/cron.weekly", "/etc/crontab"]
      expected_state: "exact"
      expected_value: "root\nroot\nroot\nroot\nroot\nroot"
      description: "Verify cron directories/files are group-owned by root"
    remediation:
      commands:
        - "chgrp root /etc/cron.d /etc/cron.daily /etc/cron.hourly /etc/cron.monthly /etc/cron.weekly /etc/crontab"
      requires_restart: false
      requires_reboot: false
      description: "Set group ownership of cron configs to root"
    rollback:
      commands: []
      description: "Manual rollback"

  debian_family:
    os: ["debian", "ubuntu"]
    check:
      type: command
      command: ["stat", "-c", "%G", "/etc/cron.d", "/etc/cron.daily", "/etc/cron.hourly", "/etc/cron.monthly", "/etc/cron.weekly", "/etc/crontab"]
      expected_state: "exact"
      expected_value: "root\nroot\nroot\nroot\nroot\nroot"
      description: "Verify cron directories/files are group-owned by root"
    remediation:
      commands:
        - "chgrp root /etc/cron.d /etc/cron.daily /etc/cron.hourly /etc/cron.monthly /etc/cron.weekly /etc/crontab"
      requires_restart: false
      requires_reboot: false
      description: "Set group ownership of cron configs to root"
    rollback:
      commands: []
      description: "Manual rollback"
